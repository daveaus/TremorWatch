<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Base application theme - using AppCompat theme for AppCompatActivity -->
    <!-- Actual theming is done via Compose MaterialTheme in code -->
    <style name="Theme.TremorWatch.Phone" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Primary brand color -->
        <item name="colorPrimary">@android:color/holo_blue_dark</item>
        <item name="colorAccent">@android:color/holo_blue_light</item>

        <!-- Status bar color - black -->
        <item name="android:statusBarColor">@android:color/black</item>
        <item name="android:windowLightStatusBar">false</item>

        <!-- Ensure window background is set to black to match status bar -->
        <item name="android:windowBackground">@android:color/black</item>
    </style>
</resources>
